# Changelog

## 1.5.1

- 修复 Minecraft 1.21.11 Yarn 映射下 `RenderPipelines` 包名错误。
- 不再调用 1.21.11 中私有的 `RenderPipelines.register` 和不存在的 `DEBUG_FILLED_SNIPPET`。
- 透墙管线改为复制 `DEBUG_FILLED_BOX` 的公开参数，再覆盖深度设置。
- 将 `DynamicUniforms.writeTransform(...)` 修正为 1.21.11 的 `write(...)`。

# 更新日志

## 1.5.1（第六版）

### 性能

- 重写世界高亮渲染器，采用 `16×16×16` 区段缓存。
- 玩家移动时复用已有区段，仅重建新进入范围或 LOD 变化的区段。
- 加入区段 AABB 与球体条件的提前剪枝。
- 仅生成暴露面，删除相邻高亮方块之间的内部面。
- 加入按距离变化的 `1 / 2 / 4 / 8` 格 LOD。
- 半透明方块模式使用稀疏内部体积标记，避免生成所有内部立方体。
- 将结果网格上传到持久化 GPU 顶点缓冲区，普通帧不再重复构建顶点。
- 透明度改为通过 GPU 颜色调制实时调整，不触发几何重算或顶点重传。
- 最大可见面数设为 300000，避免极端结果无限提交。

### 隔墙透视

- 新增专用无深度测试渲染管线。
- 关闭深度写入并关闭面剔除，保证从墙后或结果内部也能看见高亮。
- 在显示设置页新增“隔墙透视：开启/关闭”按钮。
- 开关只切换管线，不重建网格。
- 新旧配置默认开启。

### UI

- 显示设置页重新排列渲染样式和隔墙透视按钮。
- 复核 480×257 GUI 尺寸，按钮、说明文字、状态文字和完成按钮不重叠。
- 帮助页补充透视开关说明。

## 1.4.0（第五版）

- 新增 `>250` 差集条件。
- 保留普通球壳交集、第三个条件起渲染等规则。
